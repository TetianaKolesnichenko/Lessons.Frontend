// Функция для определения большего значения из двух чисел
function findMax(a, b) {
    return a > b ? a : b;
}

// Вызов функции и присвоение результата переменной
const result = findMax(10, 20);

// Вывод результата в консоль
console.log("Большее значение:", result);